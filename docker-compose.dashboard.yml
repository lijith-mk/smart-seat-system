version: '3.8'

services:
  seatsmart-dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dashboard
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - SERIAL_PORT=none